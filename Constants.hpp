#pragma once
#include <SFML/Graphics.hpp>

extern float GW, GH, WORLD_HALF_W, WORLD_HALF_H;

const float GPI        = 3.14159265f;
const float PLAYER_SPD = 220.f;
const float PLAYER_R   = 28.f;
const int   STAR_COUNT = 350;

const sf::Color COL_CORE(0, 240, 180);
